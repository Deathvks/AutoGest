'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    // El contenido de esta migración ha sido modularizado en ficheros individuales.
    // Este fichero se mantiene para conservar el historial de migraciones, pero ya no ejecuta ninguna acción.
  },

  async down(queryInterface, Sequelize) {
    // Las operaciones de "down" deben ejecutarse en el orden inverso de los nuevos ficheros de migración.
  }
};